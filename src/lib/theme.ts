// The one place a hex value lives outside globals.css: the browser chrome
// theme colour is metadata, not CSS, so it can't read a token. Keep it equal
// to --color-concrete in src/app/globals.css.
export const THEME_COLOR = "#E9E7E1";
